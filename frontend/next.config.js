/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['www.excel-communications.com', 'www.kindercare.com'],
    },
}

module.exports = nextConfig
